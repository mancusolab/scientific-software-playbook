---
description: Start collaborative design process; ingest scientific kickoff handoff when present
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, Task, WebSearch, WebFetch
model: sonnet
argument-hint: "<slug>"
---

Use your Skill tool to engage the `starting-a-design-plan` skill. Follow it exactly as written.

Pass `$1` as the required design plan slug. If the user did not provide a slug, stop and ask for one.
