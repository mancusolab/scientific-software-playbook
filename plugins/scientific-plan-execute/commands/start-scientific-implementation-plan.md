---
description: Compatibility alias for start-implementation-plan
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, Task, WebSearch, WebFetch
model: sonnet
argument-hint: "<design-plan-path> [implementation-slug]"
---

Use your Skill tool to engage the `starting-an-implementation-plan` skill. Follow it exactly as written.

Pass `$1` as the required design plan path. If `$2` is provided, treat it as a preferred implementation slug override.
